{
  "name": "amplifi-instigators-listener",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ amplifi-instigators-listener",
    "create-local": "graph create --node http://localhost:8020/ amplifi-instigators-listener",
    "remove-local": "graph remove --node http://localhost:8020/ amplifi-instigators-listener",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 amplifi-instigators-listener",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.36.1",
    "@graphprotocol/graph-ts": "0.28.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
